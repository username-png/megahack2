

<div class="nav mb-2 mt-2">
   <div class="card-header bg-dark text-white mt-2 mb-2 shadow  rounded ">
        <h3 class="card-title"> Produtos Cadastrados </h3>
    </div>
    <div class="nav-item ml-auto mr-4 mt-2">
        <a href="produtos/add"><button class="btn btn-success text-white shadow">Adicionar novo</button></a>
    </div>
</div>

<div class="row ">
    <div class="col-md-12 table-responsive ">
        <table class="table table-striped">
            <thead class="thead-dark">
                <th scope="col">Nome</th>
                <th scope="col">Categoria</th>
                <th scope="col">Cor</th>
                <th scope="col">Tamanho</th>
                <th scope="col">Estoque</th>
                <th scope="col">Preço</th>
            </thead>
            <tbody>
                {{#each produtos}}
                <tr>
                    <td>{{nome_produto}}</td>
                    <td>{{categoria}}</td>
                    <td>
                        {{#each descricao}}
                        <p> {{cor}} </p>
                        {{else}} {{/each}}
                    </td>
                     <td>
                        {{#each descricao}}
                        <p> {{tamanho}} </p>
                        {{else}} {{/each}}
                    </td>
                    <td>
                        {{#each descricao}}
                        <p> {{estoque}} </p>
                        {{else}} {{/each}}
                    </td>
                    <td>
                        {{#each descricao}}
                        <p> {{preco}} </p>
                        {{else}} {{/each}}
                    </td>
                </tr>
                {{else}}
                <tr>
                    <td colspan="7">
                        Nenhum Produto Cadastrado!
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>
